<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!--<style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />-->


    <style name="AppBarLayout_red" parent="Widget.Design.AppBarLayout">

    </style>
    <!--New base theme-->
    <style name="RedTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/PrimaryColor_red</item>
        <item name="colorPrimaryDark">@color/darkPrimaryColor_red</item>
        <item name="colorAccent">@color/accentColor_red</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="RedPopupOverlay" parent="@style/RedThemeOverlay_toolbar">
        <item name="android:colorBackground">@color/PrimaryColor_red</item>
    </style>

    <style name="RedThemeOverlay_toolbar" parent="ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/white</item>

    </style>

    <style name="ButtonNoShadow_NoBorder" parent="@style/Widget.AppCompat.Button.Borderless">
        <item name="android:background">@drawable/shape_button_no_border_light</item>
    </style>

    <style name="ButtonNoShadow_HaveBorder" parent="@style/Widget.AppCompat.Button.Borderless">
        <item name="android:background">@drawable/shape_button_have_border_light</item>
    </style>

    <!-- ProgressBar  -->
</resources>
